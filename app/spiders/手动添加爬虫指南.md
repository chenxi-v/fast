# 手动添加爬虫指南

## 方法一：仅添加 Python 文件（推荐快速测试）

### 步骤 1：添加爬虫文件
将 `.py` 爬虫文件放入 `Fastapi/app/spiders/` 目录

```
spiders/
├── my_spider.py      # 文件名 = 爬虫 key
├── movie_spider.py   # 文件名 = 爬虫 key
└── tv_spider.py      # 文件名 = 爬虫 key
```

### 步骤 2：重启后端服务
```bash
# 停止当前服务 (Ctrl+C)
# 重新启动
python -m uvicorn app.main:app --reload
```

### 步骤 3：验证加载
访问 http://localhost:8000/api/spiders 查看爬虫列表

**自动生成的配置**：
```json
{
  "my_spider": {
    "name": "my_spider",        // 默认使用文件名作为名称
    "enabled": true,
    "source": "unknown",
    "script_url": "",
    "file_path": "spiders/my_spider.py"
  }
}
```

## 方法二：添加文件并配置名称（推荐生产使用）

### 步骤 1：添加爬虫文件
```
spiders/
├── wawa.py          # 哇哇影视爬虫
└── ckzy.py          # CK 资源爬虫
```

### 步骤 2：编辑 spiders.json
```json
{
  "wawa": {
    "name": "哇哇影视",
    "enabled": true,
    "source": "upload",
    "script_url": "",
    "file_path": "spiders/wawa.py"
  },
  "ckzy": {
    "name": "CK资源",
    "enabled": true,
    "source": "upload",
    "script_url": "",
    "file_path": "spiders/ckzy.py"
  }
}
```

### 步骤 3：重启服务或调用重载 API
```bash
# 方法 1：重启服务
python -m uvicorn app.main:app --reload

# 方法 2：调用重载 API（无需重启）
curl -X POST http://localhost:8000/api/spiders/wawa/reload
curl -X POST http://localhost:8000/api/spiders/ckzy/reload
```

## 方法三：GitHub 管理爬虫（推荐团队协作）

### 仓库结构示例
```
LongTV-backend/
├── Fastapi/
│   └── app/
│       └── spiders/
│           ├── __init__.py
│           ├── spiders.json          # 爬虫配置
│           ├── spiders/              # 爬虫文件夹
│           │   ├── wawa.py
│           │   ├── ckzy.py
│           │   └── template.py       # 爬虫模板
│           └── spiders_example.json  # 配置示例
└── README.md
```

### 工作流程

#### 1. 克隆仓库
```bash
git clone https://github.com/your-username/LongTV-backend.git
cd LongTV-backend/Fastapi
```

#### 2. 添加新爬虫
```bash
# 创建爬虫文件
cd app/spiders
cat > new_spider.py << 'EOF'
class Spider:
    # ... 爬虫代码 ...
EOF
```

#### 3. 更新配置文件
编辑 `spiders.json`：
```json
{
  "new_spider": {
    "name": "新爬虫名称",
    "enabled": true,
    "source": "upload",
    "script_url": "",
    "file_path": "spiders/new_spider.py"
  }
}
```

#### 4. 提交到 GitHub
```bash
git add app/spiders/new_spider.py
git add app/spiders/spiders.json
git commit -m "添加新爬虫：新爬虫名称"
git push origin main
```

#### 5. 服务器拉取更新
```bash
# 在服务器上
cd /path/to/LongTV-backend
git pull origin main

# 重启服务或调用重载 API
systemctl restart longtv-backend
# 或
curl -X POST http://localhost:8000/api/spiders/new_spider/reload
```

## 配置字段说明

### spiders.json 字段

| 字段 | 类型 | 必需 | 说明 | 示例 |
|------|------|------|------|------|
| `name` | string | 是 | 爬虫显示名称 | "哇哇影视" |
| `enabled` | boolean | 是 | 是否启用 | true / false |
| `source` | string | 是 | 来源类型 | "upload" / "remote" / "unknown" |
| `script_url` | string | 否 | 远程脚本 URL | "https://..." |
| `file_path` | string | 是 | 文件路径 | "spiders/wawa.py" |

### source 字段说明

- **upload**: 本地上传的爬虫
- **remote**: 远程下载的爬虫
- **unknown**: 自动检测的爬虫（手动添加）

## 爬虫文件要求

### 必需实现的类和方法

```python
class Spider:
    def __init__(self):
        self.siteUrl = ""      # 网站 URL
        self.cookie = ""       # Cookie（可选）
        self.header = {}       # 请求头（可选）
    
    def init(self, extend=""):
        """初始化"""
        pass
    
    def homeContent(self, filter=None):
        """首页分类"""
        return {'class': [], 'filters': {}}
    
    def homeVideoContent(self):
        """首页推荐"""
        return {'list': []}
    
    def categoryContent(self, tid, pg, filter=None, extend=None):
        """分类列表"""
        return {'list': [], 'page': 1, 'pagecount': 1}
    
    def detailContent(self, ids):
        """视频详情"""
        return {'list': []}
    
    def searchContent(self, keyword, quick=False):
        """搜索"""
        return {'list': []}
    
    def playerContent(self, flag, vid):
        """播放地址"""
        return {'url': '', 'parse': 0}
```

## 常见问题

### Q1: 添加文件后看不到爬虫？
**A**: 检查以下几点：
1. 文件名是否正确（不含中文和特殊字符）
2. 文件是否包含 `Spider` 类
3. Spider 类是否实现了必需方法
4. 是否重启服务或调用重载 API

### Q2: 如何批量导入多个爬虫？
**A**: 
```bash
# 复制所有 .py 文件到 spiders 目录
cp /path/to/spiders/*.py Fastapi/app/spiders/

# 重启服务
systemctl restart longtv-backend
```

### Q3: 如何禁用某个爬虫？
**A**: 编辑 `spiders.json`，设置 `enabled: false`：
```json
{
  "old_spider": {
    "name": "旧爬虫",
    "enabled": false,
    ...
  }
}
```

### Q4: 如何更新爬虫代码？
**A**: 
1. 直接修改 `.py` 文件
2. 调用重载 API：`POST /api/spiders/{key}/reload`
3. 无需重启整个服务

### Q5: 多个服务器如何同步爬虫？
**A**: 
1. 使用 Git 仓库管理
2. 各服务器定期 `git pull`
3. 调用 API 重新加载爬虫

## 自动化脚本示例

### 批量重载所有爬虫
```bash
#!/bin/bash
# reload_all_spiders.sh

BASE_URL="http://localhost:8000"

# 获取所有爬虫
SPIDERS=$(curl -s $BASE_URL/api/spiders | jq -r '.data[].key')

# 逐个重载
for key in $SPIDERS; do
    echo "重新加载: $key"
    curl -X POST $BASE_URL/api/spiders/$key/reload
    echo ""
done

echo "所有爬虫已重新加载"
```

### 监控爬虫文件变化自动重载
```bash
#!/bin/bash
# watch_spiders.sh

SPIDERS_DIR="Fastapi/app/spiders"

inotifywait -m -e modify,create,delete $SPIDERS_DIR |
while read path action file; do
    if [[ "$file" == *.py ]]; then
        key="${file%.py}"
        echo "检测到文件变化: $file"
        curl -X POST http://localhost:8000/api/spiders/$key/reload
    fi
done
```

## 最佳实践

1. **命名规范**：
   - 文件名使用小写字母和下划线：`movie_spider.py`
   - 避免中文和特殊字符
   - 保持 key 简洁有意义

2. **版本控制**：
   - 使用 Git 管理所有爬虫文件
   - 提交时附带清晰的说明
   - 定期备份 `spiders.json`

3. **测试流程**：
   - 本地测试通过后再部署
   - 使用 `/api/spider/{key}` 测试单个爬虫
   - 检查日志输出确认加载成功

4. **安全考虑**：
   - 不要在爬虫文件中硬编码敏感信息
   - 使用环境变量存储 API 密钥
   - 限制爬虫文件访问权限

5. **性能优化**：
   - 禁用不常用的爬虫
   - 定期清理无效爬虫文件
   - 监控爬虫执行性能
